{
  "name": "waqtsalat",
  "version": "1.0.0",
  "private": true,
  "description": "PWA horaires de prière — Maroc uniquement — Rite Malikite — Méthode Habous",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "fetch-dataset": "node scripts/fetch-dataset.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  }
}
